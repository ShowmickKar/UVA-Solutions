#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void solve() {
	char c;
	while(scanf("%c", &c) != EOF) {
		if (c == '\n') printf("\n");
		else printf("%c", c - 7);
	}
}

int main() {
	ios::sync_with_stdio(0);
	cin.tie(nullptr);
 
	/* int t; scanf("%d", &t);
	while(t--) */ 
		solve();
}
