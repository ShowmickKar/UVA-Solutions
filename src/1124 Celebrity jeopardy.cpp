#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void solve() {
	string s;
	while (getline(cin, s)) {
		cout << s << endl;
	}
}

int main() {
	ios::sync_with_stdio(0);
	cin.tie(nullptr);
 
	/* int t; cin >> t;
	while (t--)  */
		solve();
}

